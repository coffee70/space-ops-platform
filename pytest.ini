[pytest]
markers =
    unit: fast tests that do not require external services
    integration: tests that exercise multiple platform components together
    db: tests that require a database
    slow: tests with notably higher runtime than the rest of the suite
    adapter: adapter-specific tests
